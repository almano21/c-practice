#include <stdio.h>

int main()
{
    int number;
    int isDivisible;

    printf("Enter your integer here: ");
    scanf_s("%d", &number);

    // Produces 1 if divisible by 5; otherwise, produces 0
    isDivisible = (number % 5 == 0);

    printf("%d\n", isDivisible);

    return 0;
}
