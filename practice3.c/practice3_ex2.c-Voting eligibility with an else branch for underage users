#include <stdio.h > 

int main()
{
	// Prompt user and get integer input
	int age;
	printf("Enter your age: ");
	scanf_s("%d", &age);

	// Check voting eligibility
	if (age >= 18) {
		printf("Person can vote.");

	}
	else
	{
		printf("Person can't vote.");
	}
	return 0;
}
