#include <stdio.h > 

int main()
{
	int n;
	//get integer input from the user
	printf("Enter your number: ");
	scanf_s("%d", &n);

	//multiplication table for n from 6 to 9
	for (int i = 6; i <= 9; ++i) {
		printf("%d times %d is %d\n", n, i, n * i);

	}

	return 0;
}
